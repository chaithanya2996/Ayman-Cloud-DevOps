Docker is Containerization Platform tool 

Docker will comes in 2 ways

1. Enterprise version
2. Community Version

Open Source 
Built using Programming Language - GO LANG

==========================================================
Docker:
================


Docker commands

How to Build an Image in Docker

container should be running background

continulsy running 

ctrl + p + q

task:
----
lets create a ubunutu container 
install git, python, nginx


docker run -it --name aymanub ubuntu
apt update 
apt install git -y
apt install python -y
apt install nginx -y


if I delete the container, will I get back the container

waht ever operations, i have performed this all lost.

container -> some operations -> image

container lost 

custom image -> n no of containers


commit approach -> manual approach:
==============================

image from docker hub

from that image -> create a container

install packages or work some operations

from that container -> create a new custom image

new custom image -> create a new container -> verify..

how much time ? 

15-20 mins

Automation 

Dockerfile -> Instructions (install packages or work some operations) 

Docker will create a image

Dockerfile

ubuntu
apt update
apt install nginx -y
apt install git -y


FROM ubuntu
RUN apt update 
RUN apt install nginx -y
RUN apt install git -y

===================================
check any os image

install git, java, nginx 

using commit
using Dockerfile

static website things


===============================================================================
Docker Volume 
VOLUME:
TO MAINTAIN DATA OVER THERE.

THE DATA WHICH IS AVAILABLE WE NEED TO MAKE IT AS PERSISTENT.
DATA SHOULE BE AVAILABLE


VM -> VOLUME (DISK) ->

DELETE THE VM -> THEN THE DISK OF THE VM IS ALSO GETTING DELETED.

CREATED UBUNTU VM -> APP DATA IN C-DRIVE, D-DRIVE -> AFTER UTLILIZATION.

DELETED THE UBUNTU VM ->  COMPLETELY LOST THE VOLUME /DATA.

CREATED THE CONTAINER -> INSIDE OF CONTAINER I CREATED SOME DATA,

Q) WILL THE DATA WILL REMAINS, AFTER CONTAINER DELETION?

UBUNTU CONTTAINER -> INSIDE THE CONTAINER, I HAVE CREATED SOME DATA
DATA-> NGINX, FOLDER, FILES

DELETED THE CONTAINER -> DATA -> WILL IT AVAILABLE?

HOW DO WE MAKE IT AVAILABLE ?


VOLUMES

IN VM POINT OF VIEW, WHILE CREATING THE VM WE GET DISK OVER THERE,
THAT DISK WE CALLED IT OS DISK (C-DRIVE DISK DATA)
IT STORES ONLY APP RELEATED BINARIES, OS RELEATED BINARIES, DEPENCIEES OVER THERE.
U HAVE DB IN THE VM, THE DATA WHICH IS GENERATED BY DATABASE, WE ALWAYS PREFER TO STORE THE DATA 
IN ANOTHER DISK (D-DRIVE, E-DRIVE,...)


================================================
Publishing Docker Images 

we are creating the images in local
images we create on server.
these images will work in only in this server

git (local) --> github (internet) = to access by others
image (local) -->  dockerhub (internet) = to access by othere


create a dockerhub account
create a repo

static website things

In Azure Publishing Docker images will use ACR, Contianers - Serverless mode -> ACI 

Publishing Images will be done Public Repo : hub.docker.com, nexus, ...
Publishing Images will be done Private Repo: Azure CLoud, AWS Cloud, Nexus, JFrog, ....

Azure Cloud : Azure Contianer Registry

It consists of custom images

Try to do it in a Azure CLI 
1. Create an ACR 
2. Publish the images to ACR
3. Create an ACI 